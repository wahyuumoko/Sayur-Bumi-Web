import { Card, CardContent } from "../components/ui/card";

const StockManager = () => (
  <Card>
    <CardContent>
      <h2 className="text-xl font-semibold">Manajemen Stok</h2>
      <p>Fitur tambah/ubah stok (akan ditambahkan).</p>
    </CardContent>
  </Card>
);

export default StockManager;